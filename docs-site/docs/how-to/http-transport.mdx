---
title: Run Over HTTP Transport
sidebar_position: 6
description: "Configure ThoughtJack's HTTP/SSE transport mode instead of stdio for multi-client testing, SSE streaming, and network-based MCP attack scenarios."
---

# Run Over HTTP Transport

By default ThoughtJack uses stdio (JSON-RPC over stdin/stdout). The HTTP transport enables multi-client testing with Server-Sent Events (SSE) for server-to-client messages.

## Start with HTTP

```bash
thoughtjack run --config my_config.yaml --mcp-server 127.0.0.1:8080
```

This binds an HTTP server on `http://localhost:8080`.

## Specify host and port

```bash
# Bind to all interfaces
thoughtjack run --config my_config.yaml --mcp-server 0.0.0.0:8080

# Bind to specific interface
thoughtjack run --config my_config.yaml --mcp-server 192.168.1.100:3000
```

## Test with curl

Send an MCP `initialize` request:

```bash
curl -X POST http://localhost:8080 \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "id": 1,
    "method": "initialize",
    "params": {
      "protocolVersion": "2024-11-05",
      "capabilities": {},
      "clientInfo": { "name": "test-client", "version": "1.0.0" }
    }
  }'
```

## SSE endpoint

Server-to-client messages (notifications, requests) are delivered via SSE. Connect to the SSE stream:

```bash
curl -N http://localhost:8080/sse
```

Notifications like `tools/list_changed` arrive as SSE events.

## State scoping

With HTTP transport, multiple clients can connect simultaneously. In v0.5, each connection has independent phase state by default — Client A reaching the exploit phase doesn't affect Client B.

## When to use HTTP

| Use case | Transport |
|----------|-----------|
| Testing a single MCP client | stdio |
| Testing multiple concurrent clients | HTTP |
| Automated test suites with HTTP clients | HTTP |
| Integration with MCP clients that spawn subprocesses | stdio |
| Testing SSE streaming behavior | HTTP |

## Combining with other flags

All `run` flags work with HTTP transport:

```bash
thoughtjack run \
  --config my_config.yaml \
  --mcp-server 127.0.0.1:8080 \
  --events-file events.jsonl \
  -vv
```

Note: The `pipe_deadlock` side effect only works with stdio transport and is silently skipped on HTTP.
