# Expected verdict for A2A streaming self-test.
verdict:
  result: exploited
indicators:
  task_sent:
    result: matched
  sse_status_received:
    result: matched
  task_completed:
    result: matched
execution:
  # card(2×2) + stream request(1) + SSE connected(1) + SSE events(~3) + stream outgoing(1)
  min_trace_messages: 6
