# Expected verdict for A2A client self-test.
verdict:
  result: exploited
indicators:
  task_sent:
    result: matched
  task_completed:
    result: matched
execution:
  # card fetch(2×2) + message/send request(2) + response(2) = 8
  min_trace_messages: 4
