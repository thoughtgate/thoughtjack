# Expected verdict for A2A client self-test.
verdict:
  result: exploited
indicators:
  task_delegated:
    result: matched
execution:
  # A2A client: agent card fetch + message/send → at least 2 trace messages
  min_trace_messages: 2
