# Expected verdict for MCP tool discovery scenario.
verdict:
  result: exploited
indicators:
  server_initialized:
    result: matched
execution:
  # MCP initialize handshake → at least 2 trace messages
  min_trace_messages: 2
