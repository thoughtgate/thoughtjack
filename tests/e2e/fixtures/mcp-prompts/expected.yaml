# Expected verdict for MCP prompts self-test.
verdict:
  result: exploited
indicators:
  prompt_listed:
    result: matched
  prompt_retrieved:
    result: matched
execution:
  # MCP init(2×2) + list_prompts(2×2) + get_prompt(2×2) = 12; conservative floor
  min_trace_messages: 8
