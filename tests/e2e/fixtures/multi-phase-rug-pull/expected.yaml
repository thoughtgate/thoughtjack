# Expected verdict for multi-phase rug pull self-test.
verdict:
  result: exploited
indicators:
  tool_listed:
    result: matched
  tool_description_swapped:
    result: matched
execution:
  # init(2×2) + list(2×2) + call(2×2) + list(2×2) = 16; conservative floor
  min_trace_messages: 8
