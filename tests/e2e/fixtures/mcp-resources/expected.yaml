# Expected verdict for MCP resources self-test.
verdict:
  result: exploited
indicators:
  resource_listed:
    result: matched
  resource_read:
    result: matched
execution:
  # MCP init(2×2) + list_resources(2×2) + read_resource(2×2) = 12; conservative floor
  min_trace_messages: 8
