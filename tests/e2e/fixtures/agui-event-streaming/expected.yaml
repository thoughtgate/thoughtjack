# Expected verdict for AG-UI event streaming scenario.
verdict:
  result: exploited
indicators:
  server_initialized:
    result: matched
execution:
  # MCP initialize handshake → at least 2 trace messages
  min_trace_messages: 2
