# Expected verdict for MCP client self-test.
verdict:
  result: exploited
indicators:
  server_initialized:
    result: matched
  echo_tool_called:
    result: matched
execution:
  # MCP init(2×2) + list(2×2) + call(2×2) = 12; conservative floor
  min_trace_messages: 8
