# Expected verdict for MCP client self-test.
verdict:
  result: exploited
indicators:
  echo_tool_called:
    result: matched
execution:
  # MCP client sends: initialize, tools/list, tools/call → at least 3 trace messages
  min_trace_messages: 3
