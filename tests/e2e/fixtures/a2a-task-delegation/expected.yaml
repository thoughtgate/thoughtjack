# Expected verdict for A2A task delegation scenario.
verdict:
  result: exploited
indicators:
  agui_trigger_sent:
    result: matched
execution:
  # AG-UI trigger sends at least one event
  min_trace_messages: 2
