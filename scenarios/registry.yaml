site:
  title: "ThoughtJack Attack Catalog"
  description: "Reference catalog of attack scenarios for security testing"
  base_url: /docs/scenarios

categories:
  - id: temporal
    name: "Temporal Attacks"
    description: "Trust-building and time-based attacks that evolve during a session"
    order: 1
    scenarios:
      - scenarios/rug-pull.yaml

  # Remaining categories are empty — v0.2 scenarios archived in scenarios/archive/.
  # They will be migrated to OATF format and re-added as they become available.
