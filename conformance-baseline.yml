# MCP conformance expected failures
# Updated after Phase 1 conformance fixes — remaining gaps require code changes
server:
  # Server-initiated client interactions (logging, progress, sampling, elicitation)
  - tools-call-with-logging
  - tools-call-with-progress
  - tools-call-sampling
  - tools-call-elicitation
  # Elicitation feature variants
  - elicitation-sep1034-defaults
  - elicitation-sep1330-enums
  # Resource templates (requires resources/templates/list handler)
  - resources-templates-read
  # SSE multiple streams (needs investigation)
  - server-sse-multiple-streams
