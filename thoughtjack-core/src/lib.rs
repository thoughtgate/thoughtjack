//! `ThoughtJack` Core — shared types and configuration schema
//!
//! This crate provides the configuration types and error types shared
//! across `thoughtjack` (CLI/server) and `thoughtjack-docs` (documentation
//! generation).

pub mod config;
pub mod error;
